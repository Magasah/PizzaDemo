version: '3.8'
services:
  pizza-bot:
    build: .
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
    restart: unless-stopped
    # Uncomment and use a .env file locally (do not commit it):
    # env_file:
    #   - .env
