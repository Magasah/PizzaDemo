"""Example config for PizzaDemo.

Copy this file to `config.py` and fill your `BOT_TOKEN` there, or better
set the `BOT_TOKEN` environment variable and keep `config.py` out of VCS.
"""

BOT_TOKEN = "YOUR_BOT_TOKEN_HERE"
